# Source material — copyrighted third-party tests, keys, and slide decks.
# Kept locally for curriculum development; never committed (this repo is public).
raw/
planning/
*.pptx
*.pdf
*.docx
*.dmg
*.xlsx

# ...but DO track the generated print-ready PDFs of our own curriculum.
!curriculum/pdf/**/*.pdf

.DS_Store
**/.DS_Store

# Team-specific operational data (student/parent names, emails, coach names, the
# coach's personal Google account/Drive path) — this repo is public, so none of
# this is ever committed. Generated by tools/gen_classroom_docs.py and friends
# from tools/_team_roster.py (also gitignored). See curriculum/classroom/README.md.
curriculum/classroom/Course_Overview.md
curriculum/classroom/Attendance_Signup.md
curriculum/classroom/google_sheets_import/
tools/_team_roster.py
tools/sync_to_drive.sh

# curriculum/classroom/Welcome_Email.md is tracked on purpose, by explicit request
# (2026), despite having real coach names and live Drive folder links in it — see
# git log / CLAUDE.md for context. Not an oversight if you're looking at this rule.
